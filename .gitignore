
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# IDEs
.vscode/
.idea/
.DS_Store

# Data (we might want to keep the initial data files, but let's be careful)
# If we want to keep the json files for the first push, we don't ignore them.
# The user wants to push the project, so keeping data/ is fine.
